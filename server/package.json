{
 "type": "module",
 "scripts": {
  "start": "nodemon index.js",
  "build": "cd ../client/vuexy && npm install && npm run build && cd ../../server",
  "heroku-postbuild": "npm run build",
  "test": "echo \"Error: no test specified\" && exit 1"
 },
 "dependencies": {
  "cors": "^2.8.5",
  "express": "^4.19.2",
  "mongoose": "^8.6.1"
 }
}
